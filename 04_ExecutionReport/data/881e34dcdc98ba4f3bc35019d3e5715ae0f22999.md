# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: login.spec.ts >> Tichi Web Application - Login Flow Tests >> TC_LGN_001: Successful Login with Valid Credentials
- Location: tests\login.spec.ts:12:7

# Error details

```
TypeError: loginPage.login is not a function
```

# Page snapshot

```yaml
- generic [active] [ref=e1]:
  - generic [ref=e2]:
    - img "Login Illustration" [ref=e4]
    - generic [ref=e6]:
      - generic [ref=e7]:
        - heading "Sign in to Tichi" [level=1] [ref=e8]
        - paragraph [ref=e9]: Find your dream job or the perfect hire
      - generic [ref=e12]:
        - button "Continue with Google. Opens in new tab" [ref=e14] [cursor=pointer]:
          - generic [ref=e16]:
            - img [ref=e18]
            - generic [ref=e25]: Continue with Google
        - iframe
      - generic [ref=e30]: Or continue with
      - generic [ref=e31]:
        - generic [ref=e32]:
          - generic [ref=e34]: Email Address *
          - textbox "Email Address *" [ref=e35]:
            - /placeholder: Enter your email address
        - button "Continue" [ref=e36] [cursor=pointer]
  - region "Notifications (F8)":
    - list
  - alert [ref=e37]
```

# Test source

```ts
  1   | import { expect, test } from '@playwright/test';
  2   | import { LoginPage } from '../pages/LoginPage';
  3   | 
  4   | test.describe('Tichi Web Application - Login Flow Tests', () => {
  5   |   let loginPage: LoginPage;
  6   | 
  7   |   test.beforeEach(async ({ page }) => {
  8   |     loginPage = new LoginPage(page);
  9   |     await loginPage.navigateToLogin();
  10  |   });
  11  | 
  12  |   test('TC_LGN_001: Successful Login with Valid Credentials', async ({ page }) => {
  13  |     // Navigate and login
> 14  |     await loginPage.login('qa.test@tichi.com', 'P@ssword123');
      |                     ^ TypeError: loginPage.login is not a function
  15  | 
  16  |     // Wait for URL transition and verify redirection away from the login page (e.g. to dashboard or home page)
  17  |     await page.waitForTimeout(3000); // Wait for potential animations/API response
  18  |     
  19  |     const currentUrl = await loginPage.getUrl();
  20  |     expect(currentUrl).not.toContain('/login');
  21  |     
  22  |     // Additional verification: Sign In button in header should be gone
  23  |     const signInButton = page.locator('button:has-text("Sign In")');
  24  |     await expect(signInButton).not.toBeVisible();
  25  |   });
  26  | 
  27  |   test('TC_LGN_002: Login Failure with Invalid Password', async () => {
  28  |     // Navigate and enter email
  29  |     await loginPage.enterEmail('qa.test@tichi.com');
  30  |     await loginPage.clickContinue();
  31  | 
  32  |     // Enter invalid password
  33  |     await loginPage.enterPassword('WrongPass!');
  34  |     await loginPage.clickLogin();
  35  | 
  36  |     // Verify authentication error message
  37  |     const errorText = await loginPage.getLoginError();
  38  |     expect(errorText).toContain('Invalid login details');
  39  |   });
  40  | 
  41  |   test('TC_LGN_003: Login Failure with Unregistered Email', async () => {
  42  |     const unregisteredEmail = 'unregistered@tichi.com';
  43  |     
  44  |     // Navigate and enter unregistered email
  45  |     await loginPage.enterEmail(unregisteredEmail);
  46  |     await loginPage.clickContinue();
  47  | 
  48  |     // Verify redirection to the sign-up page with the email in query parameter
  49  |     await expect(loginPage.page).toHaveURL(/\/sign-up/);
  50  |     const currentUrl = await loginPage.getUrl();
  51  |     expect(currentUrl).toContain(`email=${encodeURIComponent(unregisteredEmail)}`);
  52  |   });
  53  | 
  54  |   test('TC_LGN_004: Empty Fields Validation', async () => {
  55  |     // Click Continue without entering email
  56  |     await loginPage.clickContinue();
  57  | 
  58  |     // Verify validation error
  59  |     const errorText = await loginPage.getEmailError();
  60  |     expect(errorText).toBe('Email is required');
  61  |   });
  62  | 
  63  |   test('TC_LGN_005: Invalid Email Format Validation', async () => {
  64  |     // Enter malformed email
  65  |     await loginPage.enterEmail('gowtham@tichi,com');
  66  |     
  67  |     // Click Continue
  68  |     await loginPage.clickContinue();
  69  | 
  70  |     // Verify HTML5 native validation blocks it or the form doesn't transition to step 2 (password remains hidden)
  71  |     const isPasswordVisible = await loginPage.passwordInput.isVisible();
  72  |     expect(isPasswordVisible).toBe(false);
  73  | 
  74  |     // Verify HTML5 validation error is active on the email element
  75  |     const isValid = await loginPage.emailInput.evaluate((el: HTMLInputElement) => el.checkValidity());
  76  |     expect(isValid).toBe(false);
  77  |   });
  78  | 
  79  |   test('TC_LGN_006: Password Complexity UI Verification', async () => {
  80  |     // Navigate and enter email to transition to password field
  81  |     await loginPage.enterEmail('qa.test@tichi.com');
  82  |     await loginPage.clickContinue();
  83  | 
  84  |     // Type a weak password violating complexity rules
  85  |     await loginPage.enterPassword('weak');
  86  | 
  87  |     // Verify password complexity rules are visible and Login button is disabled
  88  |     await expect(loginPage.uppercaseRule).toBeVisible();
  89  |     await expect(loginPage.lowercaseRule).toBeVisible();
  90  |     await expect(loginPage.numberRule).toBeVisible();
  91  |     await expect(loginPage.specialCharRule).toBeVisible();
  92  |     await expect(loginPage.lengthRule).toBeVisible();
  93  | 
  94  |     const isBtnEnabledWithWeak = await loginPage.isLoginButtonEnabled();
  95  |     expect(isBtnEnabledWithWeak).toBe(false);
  96  | 
  97  |     // Fill in a valid password meeting complexity rules
  98  |     await loginPage.enterPassword('P@ssword123');
  99  | 
  100 |     // Wait briefly for reactive UI state updates
  101 |     await loginPage.waitForTimeout(500);
  102 | 
  103 |     // Verify Login button becomes enabled
  104 |     const isBtnEnabledWithStrong = await loginPage.isLoginButtonEnabled();
  105 |     expect(isBtnEnabledWithStrong).toBe(true);
  106 |   });
  107 | 
  108 |   test('TC_LGN_007: Leading/Trailing Whitespace Trimming', async ({ page }) => {
  109 |     // Navigate and enter email with spaces
  110 |     await loginPage.enterEmail('  qa.test@tichi.com  ');
  111 |     await loginPage.clickContinue();
  112 | 
  113 |     // Enter password and log in
  114 |     await loginPage.enterPassword('P@ssword123');
```